# Coin11 控制平台项目分析报告

> 分析对象：`coin11-control-backend`（后端） + `coin11-control-frontend`（前端）
> 分析时间：2026-08-24（v2 修订：修正 `coin11_tb` 目录误判）

---

## 一、项目是什么（核心定位）

这是一套**安卓设备自动化任务控制平台**的 Web 端，基于开源项目 [coin11-tb](https://github.com/czl0325/coin11-tb) 二次开发。

- **coin11-tb（上游）**：提供淘宝 / 支付宝 / 闲鱼等 App 的自动化脚本（签到、农场、摇钱树、现金签到等），通过 ADB 在真机上运行。
- **本平台（本项目）**：把上面这些脚本包装成一个**多设备、可视化的控制后台**——在网页上管理多台安卓设备、给每台设备排任务队列、实时看设备画面和日志、设备一连上就自动跑配置好的脚本。

一句话：**后端负责调度 ADB / 跑脚本 / 推流，前端负责可视化操控。**

---

## 二、两个仓库的真实关系（重点）

| 仓库 | 角色 | 说明 |
|------|------|------|
| `coin11-control-backend` | 主仓库 | FastAPI 后端 + 内嵌前端子模块 + 内嵌脚本源码目录 |
| `coin11-control-frontend` | 子模块源 | Vue 3 前端，**独立仓库**，被 backend 以 submodule 引入 |

**关键事实**：`coin11-control-backend/frontend/` 通过 `.gitmodules` 声明为 git submodule，URL 正是 `coin11-control-frontend` 仓库。所以：

- 你机器上有两个目录其实是**同一套前端代码**：`D:\...\coin11-control-frontend`（独立克隆）和 `D:\...\coin11-control-backend\frontend`（子模块引用）。
- 修改前端时，若改的是独立目录，需要在 backend 里 `git submodule update` 同步；否则 Docker 多阶段构建拿到的还是旧前端。**这是最常见的踩坑点。**
- 生产部署：后端 `main.py` 会检查 `frontend-dist` 目录并 `StaticFiles` 挂载；由 Docker 多阶段构建时编译前端产物得到（注意是 `frontend-dist`，不是 `frontend` 子模块目录）。

---

## 三、后端（`coin11-control-backend`）

**技术栈**：Python 3.11+ / FastAPI 0.115 / Uvicorn / Pydantic-Settings / WebSockets / GitPython / adbutils / construct。

**规模**：`app/` 下约 **5500+ 行** Python。

**目录结构**：
```
app/
├── main.py                 # FastAPI 入口 + WebSocket + 生命周期（启动后台设备监视）
├── api/v1/                 # router / devices / tasks / update
├── core/config.py          # 配置（含 coin11_tb 路径解析）
├── services/               # 10 个服务模块
│   ├── device_manager.py   # ADB 设备发现（USB/WiFi）
│   ├── task_engine.py      # 任务队列引擎（FIFO、重放日志）
│   ├── auto_task_runner.py # 设备上线自动触发任务 + AutoTaskWatcher 后台循环
│   ├── screen_capture.py   # 截图流（2 FPS）
│   ├── websocket_manager.py# WS 连接/广播
│   ├── repo_manager.py     # 自动 clone/更新 coin11-tb 仓库（写入生效的 coin11_tb/）
│   ├── version_manager.py  # 自检更新
│   ├── auto_task_settings.py
│   ├── device_selector_patch.py
│   └── (websocket/...) 
├── models/ schemas/        # Pydantic 模型
├── coin11_tb/              # ⚠️ 陈旧嵌套 git 仓库，未被引用（见第五节）
└── ...
tests/                      # integration/test_headless_auto_task.py + 空 unit/
coin11_tb/                  # ✅ 生效的脚本源码目录（被 repo_manager 自动维护）
```

**核心能力**：
- 设备管理：USB/WiFi 发现、`adb pair` 无线配对、连接/断开。
- 任务队列：每台设备独立 FIFO 队列，拖拽重排、批量分配/启停、运行中保护。
- 实时推送：WebSocket 推截图（base64，2FPS）+ 日志（含 ANSI），连接时回放历史日志。
- 自动化：`AutoTaskWatcher` 每 5 秒扫描 ADB，新设备上线即自动入队并启动配置好的脚本（v0.2.1 起**不再依赖网页**）。
- DevOps：Docker 多阶段构建、GitHub Actions 自动打包、`.bat`/`.ps1` 一键启动、自检更新。

**版本**：README 标注 v0.2.1（2026-08-14），但 `pyproject.toml` 仍是 0.1.0（**版本号漂移，待修复**）。

---

## 四、前端（`coin11-control-frontend`）

**技术栈**：Vue 3 (Composition API) / TypeScript (strict) / Vite 6 / Pinia 3 / Vue Router 4 / Tailwind CSS v4 / 原生 WebSocket / vuedraggable / ansi-to-html + DOMPurify。

**规模**：`src/` 下约 **6200 行**（vue + ts）。

**结构**：
```
src/
├── views/       # DashboardView / DeviceDetailView / TaskListView / SettingsView
├── stores/      # devices / tasks / websocket / update（4 个 Pinia store）
├── components/
│   ├── layout/  # AppLayout / SideNav
│   ├── device/  # DeviceCard / DeviceGrid
│   ├── task/    # TaskQueue / ScriptSelector / 批量弹窗等
│   ├── monitor/ # ScreenViewer(实时画面) / LogViewer(终端日志)
│   └── common/  # StatusIndicator / UpdateBanner
├── router/      # 4 条路由（History 模式）
├── types/       # 集中类型定义
└── __tests__/   # 5 个 Vitest 用例
```

**设计要点**：
- 暗色科技风（主色 `#00f0ff`），终端风日志（绿字发光、ANSI 渲染 + DOMPurify 防 XSS）。
- WebSocket 多设备独立连接、30s 心跳、指数退避重连（最大 16s，最多 5 次）、分桶存储（log≤2000 条、截图仅保留最新帧）。
- 开发态 Vite 代理：`/api` → :8000，`/ws` → ws://localhost:8000。
- 测试：Vitest 覆盖 DeviceCard / TaskQueue / LogViewer / stores(devices, websocket)。

**版本**：`package.json` 标注 0.1.0。

---

## 五、修正记录：关于 `coin11_tb` 目录（v2 重要更正）

**初版报告误判**：曾认为根目录 `coin11_tb/` 是残缺死代码、生效的是 `app/coin11_tb/`。**实际相反。**

经核查（2026-08-24）：

| 目录 | 状态 | 说明 |
|------|------|------|
| `coin11_tb/`（根目录） | ✅ **生效** | `app/core/config.py` 的 `coin11_tb_path_resolved` 默认指向此目录；`repo_manager` 自动 clone/更新此处；文件更新至 2026-08。 |
| `app/coin11_tb/` | ❌ **未引用** | 是一个嵌套 git 仓库，文件停留在 2026-06，**没有任何代码引用它**（grep 确认）。属于历史遗留死目录。 |

**结论**：应清理的是 `app/coin11_tb/`（陈旧嵌套仓库），保留根目录 `coin11_tb/`。两份目录均已被 `.gitignore` 的 `coin11_tb/` 规则忽略，不会入库。

---

## 六、发现的问题与风险

| 级别 | 问题 | 状态 / 说明 |
|------|------|------------|
| 🔴 高 | **`.env` 含明文 `DEEPSEEK_API_KEY`** | 未被 git 跟踪（`.gitignore` 已覆盖），但本地明文仍是风险。已加固：改为占位符，建议立即到 DeepSeek 控制台轮换该密钥。 |
| 🟡 中 | **`app/coin11_tb/` 陈旧未引用目录** | 已确认无引用，**已删除**（保留生效的根目录 `coin11_tb/`）。 |
| 🟡 中 | **前端子模块同步陷阱** | 独立前端仓库与 backend 子模块同源。改前端后须 `git submodule update`。建议 CI 显式更新子模块。 |
| 🟢 低 | **版本号漂移** | README v0.2.1 / pyproject v0.1.0 / 前端 v0.1.0 不一致。**已统一 pyproject → 0.2.1**（以 git tag 为单一版本源更佳）。 |
| 🟢 低 | **WS 鉴权硬编码默认值** | token 默认 `coin11-control-token`。生产应强制从环境变量覆盖（已在 `.env` 增加 `WS_AUTH_TOKEN` 占位说明）。 |
| 🟢 低 | **后端单测覆盖偏薄** | `tests/unit/` 基本为空，仅 1 个集成测试。前端测试反而更完整；建议补充后端 device/task 引擎单测。 |

---

## 七、成熟度与结论

- **活跃度**：两个仓库近期（2026-08）仍有提交，后端从 v0.1.0→v0.2.1 迭代稳定，功能闭环完整（设备→队列→实时→自动化→自检更新）。
- **工程质量**：分层清晰（api / services / schemas），前端 TS strict + 组件化 + 测试，整体属于**个人/小团队但相当规整**的开源项目水平。
- **定位清晰**：这是一个"真机自动化脚本的可视化中控台"，核心价值在于把零散的 coin11-tb 脚本变成可批量、可远控、可无人值守的作业系统。

**修复进展（2026-08-24）**：
1. ✅ 重写本报告，修正 `coin11_tb` 误判。
2. ✅ 加固 `.env`（密钥占位 + 轮换提醒）。
3. ✅ 删除未引用的 `app/coin11_tb/`。
4. ✅ 统一后端版本号至 0.2.1，补充 `WS_AUTH_TOKEN` 配置说明。
5. ⬜ 后续：补充后端单测、规范前端子模块同步流程。

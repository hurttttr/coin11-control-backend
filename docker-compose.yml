version: "3.9"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: coin11-control
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - ADB_PATH=adb
      - CORS_ORIGINS=["*"]
    restart: unless-stopped
